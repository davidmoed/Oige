import React from "react";
import { Container } from "react-bootstrap";

const LightSection = ({ img, heading, text, CTASection }) => {
  return <Container className="LightSectionCont">LightSection</Container>;
};

export default LightSection;
